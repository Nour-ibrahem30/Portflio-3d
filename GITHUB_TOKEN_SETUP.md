# 🔑 كيفية إعداد GitHub Token لحل مشكلة المشاريع

## 🚨 المشكلة:
المشاريع مش ظاهرة بسبب **GitHub API Rate Limit**
- بدون Token: 60 طلب/ساعة فقط ❌
- مع Token: 5000 طلب/ساعة ✅

---

## ✅ الحل السريع (5 دقائق):

### الخطوة 1: إنشاء GitHub Token

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط على **"Generate new token"** → **"Generate new token (classic)"**
3. أعطي الـ Token اسم مثل: `Portfolio-API-Access`
4. اختر **Expiration**: 90 days (أو No expiration)
5. اختر **Scopes**: 
   - ✅ فقط اختر `public_repo` (للمستودعات العامة)
6. اضغط **"Generate token"**
7. **انسخ الـ Token فوراً** (لن تراه مرة أخرى!)

---

### الخطوة 2: إضافة الـ Token في المشروع

1. افتح ملف `.env` في جذر المشروع
2. أضف الـ Token بعد `VITE_GITHUB_TOKEN=`:

```env
VITE_GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

3. احفظ الملف

---

### الخطوة 3: إعادة تشغيل المشروع

```bash
# أوقف الـ server (Ctrl+C)
# ثم شغله من جديد
npm run dev
```

---

## 🎉 النتيجة:

بعد إضافة الـ Token:
- ✅ المشاريع هتظهر بشكل طبيعي
- ✅ 5000 طلب/ساعة (بدلاً من 60)
- ✅ لن تواجه مشكلة Rate Limit مرة أخرى

---

## 🔒 ملاحظات أمان:

1. **لا تشارك الـ Token مع أحد**
2. **لا ترفع ملف `.env` على GitHub** (موجود في `.gitignore`)
3. **الـ Token آمن** - يعطي صلاحية قراءة المستودعات العامة فقط

---

## 🐛 استكشاف الأخطاء:

### المشاريع لسه مش ظاهرة؟

1. **تأكد من الـ Token صحيح:**
   - يبدأ بـ `ghp_`
   - لا توجد مسافات قبله أو بعده

2. **تأكد من إعادة تشغيل الـ server:**
   ```bash
   # أوقف الـ server (Ctrl+C)
   npm run dev
   ```

3. **افتح Console في المتصفح:**
   - اضغط F12
   - اذهب لـ Console
   - ابحث عن رسائل الخطأ

4. **تحقق من الـ Token في GitHub:**
   - اذهب إلى: https://github.com/settings/tokens
   - تأكد من أن الـ Token موجود ونشط

---

## 📝 مثال على ملف .env الصحيح:

```env
# GitHub API Token
VITE_GITHUB_TOKEN=ghp_1234567890abcdefghijklmnopqrstuvwxyz

# Google Analytics (Optional)
VITE_GA_MEASUREMENT_ID=

# Site URL
VITE_SITE_URL=https://nour-ibrahem.com
```

---

## 💡 نصائح إضافية:

### إذا كنت لا تريد استخدام Token:
- المشاريع ستظهر ولكن ببطء
- قد تواجه خطأ 403 بعد 60 طلب
- يُنصح بشدة باستخدام Token

### إذا انتهت صلاحية الـ Token:
1. اذهب إلى: https://github.com/settings/tokens
2. احذف الـ Token القديم
3. أنشئ token جديد
4. حدّث ملف `.env`

---

## ✅ التحقق من نجاح الإعداد:

بعد إضافة الـ Token وإعادة تشغيل المشروع، افتح Console (F12) وابحث عن:

```
✅ Using GitHub Token
✅ Fetched X repositories
✅ Successfully loaded X projects
```

إذا رأيت هذه الرسائل، فالإعداد نجح! 🎉

---

**آخر تحديث:** الآن
**الحالة:** جاهز للتطبيق
